{
  "name": "ragappv3-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright e2e acceptance checks for issue #573 (chat UI parity) — runs the built app against a zero-dependency stub backend on :9090.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
